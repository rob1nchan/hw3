周婧扬 - 25361053 - 第三次人工智能编程作业
1. 任务拆解与 AI 协作策略
在本次作业中，我采取了“任务拆解、分段写作、规范把控”的协作策略。

任务拆解：并未一次性要求 AI 生成全部代码，而是将作业拆分为：数据预处理（任务1）、基础统计可视化（任务2）、自定义函数设计（任务3）、复杂指标计算（任务4）、文件IO操作（任务5）及多维度关联分析（任务6）六个独立阶段。

分段协作：每完成一个任务即要求 AI 提供代码片段，在 PyCharm 中运行通过并进行 Git 提交后，再进行下一阶段，确保了代码的健壮性和可追溯性。

规范把控：在协作过程中，我特别强调了对特定库（如 NumPy 处理统计、Matplotlib 处理柱状图）的使用要求，引导 AI 严格遵守函数签名规范。

2. 核心 Prompt 迭代记录
在任务 6（服务绩效热力图）的开发中，经历了明显的 Prompt 迭代过程：

初代 Prompt：“请完成任务 6 的热力图分析代码。”

AI 生成的问题：AI 仅根据平均站点数生成了简单的热力图，忽略了作业要求中“Top 10 司机、线路、站点、车辆”四个维度的排名统计。

优化后的 Prompt：“请重新完成任务 6。要求：1. 分别统计服务人次最多的 Top 10 司机、线路、上车站点和车辆。2. 构造一个 4x10 的矩阵。3. 使用 seaborn 绘制热力图，行标签为司机、线路等，列标签为 Top1~Top10。4. 包含不少于 50 字的规律说明。”

结果：最终生成的代码完全覆盖了作业文档的所有细节要求，顺利通过评审。

3. Debug 记录
报错现象：运行任务 1 时出现 KeyError: '交易时间'，且显示数据集大小只有 1 列。

解决过程：

1.观察现象：发现 AI 默认使用了制表符 \t 作为分隔符，但通过观察控制台输出的前 5 行数据，发现所有字段被挤在了一起。

2.定位问题：确认原始 ICData.csv 文件虽然在文档中说是制表符分隔，但实际数据是用逗号 , 分隔的。

3.修复方案：将 pd.read_csv 的参数改为 sep=','。

4.反思：作为小白，我学会了在报错时先看 df.head() 的输出格式，这比盲目修改代码更有效。

4. 人工代码审查（逐行中文注释）
以下是任务 4 中关于高峰小时系数（PHF）计算的核心代码及我的逐行审查注释：

Python
# 找出刷卡量最大的小时作为高峰小时，idxmax() 返回最大值所在的索引（即小时数）
peak_hour = int(hourly_total.idxmax())
peak_count = int(hourly_total[peak_hour]) # 获取该高峰小时内的总刷卡量

# 筛选出属于高峰小时的数据，并设置时间索引，为接下来的重采样做准备
df_peak = df[df['hour'] == peak_hour].set_index('交易时间').sort_index()

# 使用 resample('5min') 将 60 分钟划分为 12 个 5 分钟片段，统计每段的刷卡数
resample_5 = df_peak.resample('5min').size()
max_5min = int(resample_5.max()) # 找到这 12 个片段中的最大值

# 根据 PHF5 公式计算：高峰小时总量 / (12个片段 * 最大片段量)
phf5 = peak_count / (12 * max_5min)

# 同理，使用 resample('15min') 将 60 分钟划分为 4 个 15 分钟片段
resample_15 = df_peak.resample('15min').size()
max_15min = int(resample_15.max()) # 找到这 4 个片段中的最大值

# 根据 PHF15 公式计算：高峰小时总量 / (4个片段 * 最大片段量)
phf15 = peak_count / (4 * max_15min)